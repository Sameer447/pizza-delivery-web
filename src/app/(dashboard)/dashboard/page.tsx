import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { PageContainer } from "@/components/layout/page-container";
const cards = [{ label: "Orders", value: "—" }, { label: "Revenue", value: "—" }, { label: "Restaurants", value: "—" }, { label: "Customers", value: "—" }];
export default function DashboardPage() { return <PageContainer title="Dashboard" description="Welcome back. Here is a snapshot of your workspace."><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map((card) => <Card key={card.label}><CardHeader className="pb-2"><CardTitle className="text-sm font-medium text-muted-foreground">{card.label}</CardTitle></CardHeader><CardContent><p className="text-3xl font-semibold">{card.value}</p><p className="mt-1 text-xs text-muted-foreground">Placeholder metric</p></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Recent Activity</CardTitle></CardHeader><CardContent><p className="text-sm text-muted-foreground">Activity will appear here once platform data is connected.</p></CardContent></Card></PageContainer>; }
