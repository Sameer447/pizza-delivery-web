"use client";
import Link from "next/link";
import { Mail, ArrowLeft } from "lucide-react";
import { useForm } from "react-hook-form";
import { AuthBrandPanel, MobileBrand } from "@/components/auth/auth-brand-panel";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export default function ForgotPasswordPage() { const { register, handleSubmit, formState: { isSubmitting } } = useForm<{ email: string }>(); return <main className="flex min-h-screen w-full bg-background text-foreground"><AuthBrandPanel><h1 className="type-display mb-4">Operational Excellence.</h1><p className="type-body-lg text-muted-foreground">Streamline your high-volume food service environments with precise, reliable, and fast management tools.</p></AuthBrandPanel><section className="relative flex w-full items-center justify-center p-8 sm:p-12 md:p-24 lg:w-1/2"><div className="w-full max-w-md space-y-8"><MobileBrand /><div><h1 className="type-page-title mb-2">Forgot your password?</h1><p className="text-body-reg text-muted-foreground">Enter your email address and we&apos;ll send you a link to reset your password.</p></div><form className="space-y-6" onSubmit={handleSubmit(async () => {})}><div className="space-y-1"><label className="type-label-caps block" htmlFor="email">Email Address</label><div className="relative"><Mail className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input id="email" type="email" autoComplete="email" placeholder="chef@kitchen.com" required className="pl-10" {...register("email")} /></div></div><Button className="w-full" type="submit" disabled={isSubmitting}>Send Reset Link</Button></form><Link className="flex items-center justify-center gap-1 text-body-sm font-medium text-primary hover:text-primary-container" href="/login"><ArrowLeft className="h-4 w-4" />Back to Sign In</Link></div><div className="absolute bottom-0 left-0 h-1 w-full bg-gradient-to-r from-transparent via-primary to-transparent opacity-30" /></section></main>; }
