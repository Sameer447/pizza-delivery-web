"use client";
import { LogOut, Menu } from "lucide-react";
import { useAuth } from "@/providers/auth-provider";
import { ThemeSwitcher } from "@/components/theme/theme-switcher";
export function Header({ onMenuClick }: { onMenuClick?: () => void }) { const { user, logout } = useAuth(); return <header className="flex h-16 items-center justify-between border-b bg-surface-lowest px-4 sm:px-6"><button onClick={onMenuClick} className="rounded-[var(--radius)] p-2 lg:hidden" aria-label="Open navigation"><Menu className="h-5 w-5" /></button><div className="ml-auto flex items-center gap-4"><ThemeSwitcher /><div className="hidden text-right sm:block"><p className="text-sm font-medium">{user ? `${user.firstName} ${user.lastName}` : "Admin"}</p><p className="text-xs text-muted-foreground">{user?.role ?? "Session"}</p></div><button onClick={() => void logout()} className="rounded-[var(--radius)] p-2 text-muted-foreground hover:bg-muted hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring" aria-label="Log out"><LogOut className="h-4 w-4" /></button></div></header>; }
