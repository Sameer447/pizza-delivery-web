"use client";
import Link from "next/link";
import { useForm } from "react-hook-form";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
type Values = { firstName: string; lastName: string; email: string; phone: string; role: string };
export function InviteForm() { const { register, handleSubmit, formState: { isSubmitting } } = useForm<Values>({ defaultValues: { role: "Restaurant Admin" } }); return <form className="space-y-6" onSubmit={handleSubmit(async () => {})}><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1 text-body-sm font-semibold">First Name<Input {...register("firstName")} placeholder="e.g. Muhammad" required /></label><label className="space-y-1 text-body-sm font-semibold">Last Name<Input {...register("lastName")} placeholder="e.g. Ali" required /></label><label className="space-y-1 text-body-sm font-semibold sm:col-span-2">Email Address<Input {...register("email")} type="email" placeholder="admin@pizzahouse.com" required /></label><label className="space-y-1 text-body-sm font-semibold sm:col-span-2">Phone Number<Input {...register("phone")} type="tel" placeholder="+92 300 1234567" required /></label><label className="space-y-1 text-body-sm font-semibold sm:col-span-2">Assigned Role<select className="h-10 w-full rounded-[var(--radius)] border bg-surface-lowest px-3 text-sm" {...register("role")}><option>Restaurant Admin</option><option>Kitchen Supervisor</option><option>Finance & Billing</option></select></label></div><div className="flex justify-end gap-3"><Link href="/administrations" className="inline-flex h-10 items-center rounded-[var(--radius)] border px-4 text-sm font-semibold hover:bg-muted">Cancel</Link><Button type="submit" disabled={isSubmitting}>Send Invitation</Button></div></form>; }
