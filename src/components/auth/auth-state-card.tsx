import Link from "next/link";
import type { LucideIcon } from "lucide-react";
export function AuthStateCard({ icon: Icon, title, message, action, href = "/dashboard", tone = "error" }: { icon: LucideIcon; title: string; message: string; action: string; href?: string; tone?: "error" | "primary" }) { return <main className="flex min-h-screen items-center justify-center bg-background p-4"><section className="w-full max-w-md rounded-[var(--radius-lg)] border bg-surface-lowest p-8 text-center shadow-[var(--shadow-soft)]"><div className={`mx-auto mb-6 flex h-16 w-16 items-center justify-center rounded-full ${tone === "error" ? "bg-destructive/10 text-destructive" : "bg-primary/10 text-primary"}`}><Icon className="h-8 w-8" aria-hidden="true" /></div><h1 className={`type-page-title mb-2 ${tone === "error" ? "text-destructive" : ""}`}>{title}</h1><p className="mb-8 text-body-reg text-muted-foreground">{message}</p><Link className="inline-flex h-10 w-full items-center justify-center rounded-[var(--radius)] bg-primary px-4 text-body-reg font-semibold text-primary-foreground transition-colors hover:bg-primary-container focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring" href={href}>{action}</Link><p className="mt-8 type-label-caps text-muted-foreground/60">Pro-Kitchen Admin</p></section></main>; }
